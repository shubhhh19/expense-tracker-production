# Database Configuration - Choose one approach:

# Option 1: Using DATABASE_URL for Neon.tech (recommended for production)
DATABASE_URL=postgresql://neondb_owner:npg_ZoxV6l5SnmEa@ep-silent-butterfly-a4iz3m98-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require

# Option 2: Using individual parameters (for local development)
DB_NAME=expense_tracker
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_key
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=10000
NODE_ENV=production

# Frontend URL for CORS
FRONTEND_URL=https://expense-tracker-frontend.netlify.app

# File Upload Configuration
MAX_FILE_SIZE=5242880  # 5MB in bytes
